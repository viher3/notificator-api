components:
  schemas:

    Notification:
      type: object
      properties:
        to:
          type: string
        from:
          type: string
        message:
          type: string
        subject:
          type: string
        isSendConfirmationRequired:
          type: bool

    Notifications:
        type: array
        items:
          $ref: '#/components/schemas/Notification'